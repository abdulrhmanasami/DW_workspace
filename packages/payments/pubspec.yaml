name: payments
description: Payment models and interfaces for Delivery Ways
version: 1.0.0
publish_to: none
environment:
  sdk: ">=3.3.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  json_annotation: ^4.9.0
  foundation_shims: ^0.1.0
  payments_shims: ^0.1.0
  payments_adapter_stripe: ^1.0.0
  payments_stub_impl: ^0.1.0
dev_dependencies:
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  test: ^1.24.0
flutter:
  uses-material-design: false
