name: notifications_shims
description: Canonical notification contracts and domain models for Delivery Ways
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  meta: ^1.11.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.24.0
