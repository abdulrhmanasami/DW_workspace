name: accounts_shims
description: Shims for user accounts and identity management via backend
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  http: ^1.2.1
  foundation_shims: ^0.1.0
  dsr_ux_adapter: ^1.0.0
  json_annotation: ^4.9.0

  crypto: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  json_serializable: ^6.7.1

flutter: null
